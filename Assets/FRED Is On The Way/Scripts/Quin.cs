using UnityEngine;

public class Quin : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        bool Needhelp = true;

        if (Needhelp)
        {
            bool Quinhelps = true;

            while (Quinhelps)
            {
                Debug.Log("thanks Quin");
            }
        }

      
    }
}
